name: ccSocial
description: Cooperation and Conflict Social Network Survey App.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  path: ^1.7.0
  sqflite: ^1.3.2+1
  path_provider: ^1.6.27
  ext_storage: ^1.0.3
  permission_handler: ^5.0.1+1

dev_dependencies:
  flutter_test:
    sdk: flutter
    
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/
# TODO: Need to programatically add assets here, because there are going to be a LOT of photos in sub directories... 